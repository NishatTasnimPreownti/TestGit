import java.lang.reflect.Array;
import java.util.ArrayList;

public class orderManager {
    public Product sortProducts(Product[] products) {
        Array.getShort(products);
        return products;

    }
}
